/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Optional: Add a custom base path for deployment to a subdirectory
  // basePath: '/viral-hook-generator',
};

module.exports = nextConfig;